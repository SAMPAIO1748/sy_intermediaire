{% extends "front/home.html.twig" %}

{% block main %}
	<h2>{{product.name}}</h2>
	<p>{{ product.description }}</p>
	<h3>{{ product.price }}</h3>
	<h3>{{ product.stock }}</h3>
	<h4>{{ product.category.name }}</h4>
{% endblock %}
