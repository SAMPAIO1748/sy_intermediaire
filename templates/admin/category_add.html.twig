{% extends "admin/home.html.twig" %}

{% block main %}

	{{ form_start(categoryForm) }}

	{{ form_label(categoryForm.name, 'Nom') }}
	{{ form_widget(categoryForm.name, {'attr' : {'placeholder' : 'Le nom de la catégorie'} } ) }}
	{{ form_label(categoryForm.description, 'Description') }}
	{{ form_widget(categoryForm.description, {'attr' : {'placeholder' : 'La description de la catégorie'} } ) }}

	{{ form_end(categoryForm) }}

{% endblock %}
